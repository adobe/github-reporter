<%@ val pulls: Seq[com.github.chetanmeh.tools.git.PullRequest] %>
<%@ val heading: String %>
<%
    val H1 = "#"
    val H3 = "#" * 3
%>
#if (pulls.nonEmpty)
${H3} ${heading}:
#for (i <- pulls)
    * ${H1}${i.id.toString} - ${i.title} (${i.creator})
#end
#end