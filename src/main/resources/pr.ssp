<%@ val pulls: Seq[com.github.chetanmeh.tools.git.PullRequest] %>
<%@ val heading: String %>
<% val H = "#" %>
#if (pulls.nonEmpty)
${H}${H}${H} ${heading}:
#for (i <- pulls)
    * ${H}${i.id.toString} - ${i.title} (${i.creator})
#end
#end